<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalhes do Cliente</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-5">
  <!-- Mensagem de notificação -->
  <% if notice %>
    <div class="alert alert-success" role="alert">
      <%= notice %>
    </div>
  <% end %>

  <!-- Renderiza os detalhes do cliente -->
  <div class="card mb-4">
    <div class="card-header">
      <h5 class="card-title">Detalhes do Cliente</h5>
    </div>
    <div class="card-body">
      <%= render @cliente %>
      <div class="mt-3">
        <p><strong>Criado em:</strong> <%= @cliente.created_at.strftime("%d/%m/%Y %H:%M") if @cliente.created_at %></p>
        <p><strong>Atualizado em:</strong> <%= @cliente.updated_at.strftime("%d/%m/%Y %H:%M") if @cliente.updated_at %></p>
      </div>

      <div class="mt-4">
        <%= link_to "Editar Cliente", edit_cliente_path(@cliente), class: "btn btn-primary" %>
        <%= link_to "Voltar para Clientes", clientes_path, class: "btn btn-secondary" %>
        <%= button_to "Excluir Cliente", @cliente, method: :delete, class: "btn btn-danger", data: { confirm: "Você tem certeza que deseja excluir este cliente?" } %>
      </div>
    </div>
  </div>
</div>

</body>
</html>
