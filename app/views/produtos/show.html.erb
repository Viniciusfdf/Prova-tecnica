<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Visualizar Produto" %></title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #f8f9fa;
          margin: 0;
          padding: 0;
      }
      header {
          background-color: #0fc639;
          color: white;
          padding: 10px 20px;
          text-align: center;
      }
      h1 {
          margin-top: 20px;
          color: #ffffff;
      }
      .container {
          padding: 20px;
      }
      .produto-info {
          background-color: white;
          padding: 20px;
          border-radius: 5px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .produto-info p {
          margin: 0;
          padding: 5px 0;
      }
      .btn-group {
          margin-top: 20px;
      }
      footer {
          background-color: #0fc639;
          color: white;
          text-align: center;
          padding: 10px;
          position: fixed;
          bottom: 0;
          width: 100%;
      }
  </style>
</head>
<body>

<header>
  <h1>Detalhes do Produto</h1>
</header>

<div class="container">
  <!-- Mensagem de notificação (se houver) -->
  <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
  <% end %>

  <!-- Informações do produto -->
  <div class="produto-info">
    <%= render @produto %>

    <div class="mt-3">
      <p><strong>Criado em:</strong> <%= @produto.created_at.strftime("%d/%m/%Y %H:%M") if @produto.created_at %></p>
      <p><strong>Atualizado em:</strong> <%= @produto.updated_at.strftime("%d/%m/%Y %H:%M") if @produto.updated_at %></p>
    </div>
  </div>

  <!-- Ações: Editar, Voltar, Excluir -->
  <div class="btn-group mt-3">
    <%= link_to "Editar Produto", edit_produto_path(@produto), class: "btn btn-primary" %>
    <%= link_to "Voltar para Produtos", produtos_path, class: "btn btn-secondary" %>
    <%= button_to "Excluir Produto", @produto, method: :delete, class: "btn btn-danger", data: { confirm: "Tem certeza que deseja excluir este produto?" } %>
  </div>
</div>

<footer>
  <p>&copy; <%= Time.now.year %> Sistema de Produtos</p>
</footer>

</body>
</html>
